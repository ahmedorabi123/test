# Backend API base URL (no trailing slash, no /api/v1 — that is appended in src/api/client.ts).
# Local dev:    http://localhost:3010
# Production:   https://<your-render-service>.onrender.com
VITE_API_URL=http://localhost:3010
